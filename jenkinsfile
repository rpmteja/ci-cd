pipeline {
    agent any 
    options {
    buildDiscarder(logRotator(numToKeepStr: '2')
    
    }
    stages {
        stage('Declaratice pipeline') {
            steps {
                echo 'Hello world!' 
            }
        }
    }
}
